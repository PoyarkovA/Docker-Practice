# 📦 Практическая работа: Дополнительные возможности Docker

## 📌 Описание

В рамках данной практической работы были изучены **продвинутые механизмы Docker**, связанные с управлением жизненным циклом контейнеров, мониторингом их состояния и оптимизацией использования системных ресурсов.

Практика ориентирована на формирование навыков, необходимых для работы с Docker в условиях, максимально приближенных к **production-среде**.

---

## ✅ Выполненные задания

### 1️⃣ Вывод логов контейнера в файл

Освоен механизм сохранения стандартных потоков вывода контейнера (**stdout** и **stderr**) в файловую систему хоста.

* Выполнено перенаправление вывода команды `docker logs` в текстовый файл
* Реализована возможность последующего анализа логов

📷 Скриншот выполнения:

<img width="980" height="421" alt="image" src="https://github.com/user-attachments/assets/137a1a84-edb3-4a4a-9355-e6dc0834508b" />

Так как я делал на windows, пришлось немного менять команды, но вывод вышел верный

🔎 **Преимущество:** данный подход позволяет выполнять постмортем-анализ и диагностику даже после остановки или удаления контейнера.

---

### 2️⃣ Мониторинг ресурсов контейнеров (Docker Stats)

Проведен анализ потребления системных ресурсов контейнерами в реальном времени с использованием утилиты `docker stats`.

Изучены следующие метрики:

* загрузка CPU
* использование оперативной памяти
* сетевой ввод-вывод

📷 Скриншот выполнения:

<img width="956" height="145" alt="image" src="https://github.com/user-attachments/assets/8198a25c-1d0b-4890-95d4-789c2166eb09" />


📌 Дополнительно реализовано сохранение **"мгновенных снимков" (snapshots)** статистики в файл без потокового обновления. Это позволяет фиксировать состояние системы под нагрузкой.

---

### 3️⃣ Ограничение потребления ресурсов

На практике применены механизмы жесткого ограничения вычислительных ресурсов контейнера для предотвращения монополизации ресурсов хоста.

Реализовано:

* ограничение оперативной памяти (`--memory`)
* ограничение процессорного времени (`--cpus`)
* динамическое изменение лимитов с помощью команды `docker update`

📷 Скриншот выполнения:

<img width="965" height="219" alt="image" src="https://github.com/user-attachments/assets/68243d6c-4170-4556-b85f-b90fb7c5db1b" />


⚙️ **Важно:** данные настройки критически необходимы для стабильной работы многоконтейнерных сред.

---

### 4️⃣ Экспорт контейнера в архив

Выполнено создание полной резервной копии файловой системы контейнера.

* Использована команда `docker export`
* Состояние работающего контейнера сохранено в переносимый `tar`-архив

📷 Скриншот выполнения:

<img width="960" height="611" alt="image" src="https://github.com/user-attachments/assets/993da8dd-ab51-4ae7-b61a-87ef458d4ab0" />


💾 Данный метод позволяет сохранить все изменения, произведенные внутри контейнера с момента запуска, и применяется для бэкапов или передачи окружения.

---

### 5️⃣ Импорт контейнера из архива

Отработан процесс восстановления рабочего окружения из ранее созданного архива.

* Использована команда `docker import`
* Создан новый Docker-образ на основе `tar`-архива
* Из образа успешно запущен контейнер

📷 Скриншот выполнения:

<img width="1079" height="980" alt="image" src="https://github.com/user-attachments/assets/b85eab89-9b78-48ce-a126-315b2c612a42" />


🔁 Восстановленный контейнер полностью идентичен исходному по состоянию файловой системы.

---

## 🏁 Итог

В ходе практической работы были закреплены ключевые навыки администрирования Docker:

* диагностика и анализ логов
* мониторинг и контроль ресурсов
* управление ограничениями контейнеров
* резервное копирование и миграция окружений

Все операции выполнялись через **CLI**, что подтверждает готовность к работе с Docker в серверном и production-окружении.

---

🚀 **Практическая работа выполнена в полном объеме.**
